'use strict';

/* eslint no-unused-vars: 0 */

module.exports.destroy = function destroy( fullPath ) {
    // TODO: Delete from bucket, then update the fs

    return Promise.reject({ code: 501, message: 'Not implemented.' });
};
